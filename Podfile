# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'graphql-ios-chat' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for graphql-ios-chat
  pod 'Apollo'

  pod 'Apollo/WebSocket'

  target 'graphql-ios-chatTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'graphql-ios-chatUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
